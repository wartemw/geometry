#ifndef H_PARS
#define H_PARS

#include <cstdlib>
#include <cstring>
#include <iostream>
extern int i;
using namespace std;

void skipspace(string a);
string partStr(string a);
int partStr2(string a);

#endif
